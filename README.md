# Database-Models-PostgreSQL-Integration
Connected FastAPI with PostgreSQL using SQLAlchemy, created database models including users, profiles, subscriptions, and POS modules. Configured database engine, sessions, environment variables, migrations, and verified the connection through PgAdmin and API-level integration testing.
